export enum Status {
    Backlog = "Backlog",
    InProgress = "InProgress",
    Done = "Done",
}

export enum FormButtonType {
    Submit = "submit",
    DropZoneButton = "drop-zone-button",
    EmptyDropZoneButton = "empty-drop-zone-button",
    TaskButton = "task-button",
}
